# Write another function called median that takes in a list of numbers and 
returns a float representing the median from that list.


### YOUR CODE STARTS HERE ###

def median(sample):
  n = len(sample)
  index = n // 2
  if n % 2:
    return sorted(sample)[index]
  return sum(sorted(sample)[index - 1:index + 1]) / 2
  


### YOUR CODE ENDS HERE ###
